<!DOCTYPE html>
<html>
  <head>
    <title>Arkhe</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" >
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700|Noto+Sans+JP:400,700" rel="stylesheet">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="wrapper clearfix">
      <header class="top">
          <div class="container">
              <nav class="nav navbar-nav navbar-left">
                <% if user_signed_in? %>
                  <%= link_to users_path do %>
                      <%= image_tag "logo2.png", class: 'mypae-image', style: "width: 200px; height: 63px; margin-top: 20px;" %>
                      <% end %>
                <% else %>
                  <span><%= link_to root_path do %>
                      <%= image_tag "logo2.png", class: 'mypae-image', style: "width: 200px; height: 63px; margin-top: 20px;" %>
                      <% end %>
                  <% end %>
                  </span>
              </nav>
                  <ul class="header-page">
                      <% if user_signed_in? && current_user.admin? %>
                          <li class="header-right">
                            <%= link_to "新規投稿", new_post_path, class: "header-link fas fa-walking" %>
                          </li>
                          <li class="header-right">
                            <%= link_to "管理者マイページ", user_path(current_user.id), class: "header-link fas fa-home" %>
                          </li>
                          <li class="header-right">
                              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "header-link fas fa-sign-out-alt" %>
                          </li>
                      <% elsif user_signed_in? && !current_user.admin? %>
                          <li class="header-right">
                            <%= link_to "新規投稿", new_post_path, class: "header-link fas fa-walking" %>
                          </li>
                          <li class="header-right">
                            <li class="header-right">
                            <%= link_to "マイページ", user_path(current_user.id), class: "header-link fas fa-home" %>
                          </li>
                          <li class="header-right">
                              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "header-link fas fa-sign-out-alt" %>
                          </li>
                      <% else %>
                          <li class="header-right">
                              <%= link_to "新規会員登録", new_user_registration_path, class: "header-link fas fa-user-plus" %>
                          </li>
                          <li class="header-right">
                              <%= link_to "ログイン", new_user_session_path, class: "header-link fas fa-sign-in-alt" %>
                          </li>
                      <% end %>
                  </ul>
          </div>
      </header>
      <%# if current_user.deleted == false %>
        <%= yield %>
      <%# else  %>
        <!-- <p>このユーザーは未登録、もしくは退会済みです</p> -->
      <%# end  %>
      <footer class="footer">
          <p class="footer-text">© Yumiko Fukuchi.</p>
      </footer>
    </div>

  </body>
</html>